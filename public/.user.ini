; Increase upload limits for container photos (fixes 413 Request Entity Too Large).
; Note: Nginx also needs client_max_body_size 50M; if using Apache, check LimitRequestBody.
upload_max_filesize = 50M
post_max_size = 50M
